# Smart Weather

Smart Weather is a Node.js backend project for a weather-based application.

## Getting Started

1. Install dependencies:

```bash
npm install
```

2. Create a `.env` file (already scaffolded) and update values as needed.

3. Run the development server:

```bash
node server.js
```

